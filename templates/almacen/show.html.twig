{% extends 'base.html.twig' %}

{% block title %}Almacen{% endblock %}

{% block slidebar %}
    {{ include('view/sidebarnav/menu.html.twig') }}
{% endblock %}

{% block body %}
    <div class="row justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Cliente</h4>

                    <button type="button" class="btn btn-dark btn-outline" onclick="location.href='{{ path('almacen_index') }}'"><i class="fa fa-angle-left "></i> back to list</button>

                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ almacen.id }}</td>
                        </tr>
                        <tr>
                            <th>Nombre</th>
                            <td>{{ almacen.nombre }}</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="row justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <button type="button" class="btn btn-info btn-block" onclick="location.href='{{ path('almacen_edit', {'id': almacen.id}) }}'"> <i class="fa fa-pencil"></i> Edit</button>

                    <br>
                    {{ include('almacen/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}