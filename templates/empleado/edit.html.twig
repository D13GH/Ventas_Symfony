{% extends 'base.html.twig' %}

{% block title %}Edit Empleado{% endblock %}

{% block slidebar %}
    {{ include('view/admin/menu.html.twig') }}
{% endblock %}

{% block body %}
    <div class="row justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Edit Empleado</h4>
                    <button type="button" class="btn btn-dark btn-outline" onclick="location.href='{{ path('empleado_index') }}'"><i class="fa fa-angle-left "></i> back to list</button>


                    {{ include('empleado/_form.html.twig', {'button_label': 'Update'}) }}



                    {{ include('empleado/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}