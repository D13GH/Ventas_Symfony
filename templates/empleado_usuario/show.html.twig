{% extends 'base.html.twig' %}

{% block title %}EmpleadoUsuario{% endblock %}

{% block body %}
    <h1>EmpleadoUsuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empleado_usuario.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empleado_usuario_index') }}">back to list</a>

    <a href="{{ path('empleado_usuario_edit', {'id': empleado_usuario.id}) }}">edit</a>

    {{ include('empleado_usuario/_delete_form.html.twig') }}
{% endblock %}