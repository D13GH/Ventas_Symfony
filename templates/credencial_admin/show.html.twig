{% extends 'base.html.twig' %}

{% block title %}Credencial Admin{% endblock %}

{% block slidebar %}
    {{ include('view/sidebarnav/menu.html.twig') }}
{% endblock %}

{% block body %}
    <div class="row justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Credencial Admin</h4>

                    <button type="button" class="btn btn-dark btn-outline" onclick="location.href='{{ path('credencial_admin_index') }}'"><i class="fa fa-angle-left "></i> back to list</button>

                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ credencial_admin.id }}</td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td>{{ credencial_admin.email }}</td>
                        </tr>
                        <tr>
                            <th>Contraseña</th>
                            <td>{{ credencial_admin.pass }}</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="row justify-content-center ">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-body">
                    <button type="button" class="btn btn-info btn-block" onclick="location.href='{{ path('credencial_admin_edit', {'id': credencial_admin.id}) }}'"> <i class="fa fa-pencil"></i> Edit</button>

                    <br>
                    {{ include('credencial_admin/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}